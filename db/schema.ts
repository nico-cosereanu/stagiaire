/*
 * Drizzle schema — all tables for Stagiaire.
 * See PLAN.md §14 for the source-of-truth data model.
 *
 * Empty for now; tables land in the next checkpoint.
 * The empty namespace is still valid as Drizzle's `schema` argument.
 */

export {};
